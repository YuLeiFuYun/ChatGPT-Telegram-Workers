name: Sync Fork

on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.8
        with:
          token: ${{ secrets.CHATGPT_TELEGRAM_TOKEN }}
          owner: TBXark
          base: master
          head: master
